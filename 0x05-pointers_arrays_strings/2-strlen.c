#include "main.h"

/**
 *  _strlen - return length of characters
 *  @s: character to check
 *  Return: 0 always
 */

int _strlen(char *s)
{
	 int i;

	for (i = 0; s[i] != '\0'; i++)

	return (i);

}
