#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * @n - any integer whose abs value is taken
 * Return: 'n' without negative number
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (n);
}
